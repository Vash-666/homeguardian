# HomeGuardian Critical Services Configuration
# Format: service_name,check_type,check_command,expected_status

# System Services
[System]
openclaw,systemd,openclaw,active
docker,systemd,docker,active
ssh,systemd,ssh,active
cron,systemd,cron,active

# Network Services
[Network]
nginx,systemd,nginx,active
tailscale,systemd,tailscale,active
unbound,systemd,unbound,active

# Application Services
[Applications]
homeassistant,docker,homeassistant,running
node-red,docker,node-red,running
mosquitto,docker,mosquitto,running
zigbee2mqtt,docker,zigbee2mqtt,running

# Database Services
[Database]
postgresql,systemd,postgresql,active
redis,systemd,redis,active
influxdb,docker,influxdb,running

# Monitoring Services
[Monitoring]
grafana,docker,grafana,running
prometheus,docker,prometheus,running
alertmanager,docker,alertmanager,running

# Custom Health Checks
[Custom]
openclaw_gateway,http,http://localhost:18789/health,200
homeassistant_api,http,http://localhost:8123/api/,200
grafana_dashboard,http,http://localhost:3000,200
prometheus_metrics,http,http://localhost:9090/metrics,200

# Port Checks
[Ports]
ssh_port,tcp,localhost:22,open
http_port,tcp,localhost:80,open
https_port,tcp,localhost:443,open
mqtt_port,tcp,localhost:1883,open